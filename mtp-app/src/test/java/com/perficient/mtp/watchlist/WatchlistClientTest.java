package com.perficient.mtp.watchlist;

public class WatchlistClientTest {
/*
	private MockWebServer mockWebServer;
	private TwilioClient twilioClient;

	@BeforeEach
	void setupMockWebServer() {
		mockWebServer = new MockWebServer();

		TwilioClientProperties properties = new TwilioClientProperties();
		properties.setBaseUrl(mockWebServer.url("/").url().toString());
		properties.setAccountSid("ACd936ed6d");

		twilioClient = new TwilioClient(WebClient.create(), properties);
	}

	@Test
	public void testGetWatchlistList() {

		WatchlistClient service = new WatchlistClient();

		Mono<WatchlistList> profile = service.getWatchlists();

		assertNotNull(profile);
		assertEquals("TSLA", profile.getSymbol());
		assertEquals(30, profile.getQuoteList().size());
	}
	*/
}
